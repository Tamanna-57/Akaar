<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Photographing the product, and describing it by voice. Both are
         requested at the moment they are first needed, with the reason shown
         in her own words - see core-media/src/permissions.ts. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- The camera is central to the app, but a device without one should
         still install and run the buyer side. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Lets the outbox keep draining after the app is swiped away. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">

      <!--
        CERTIFICATE PINNING IS NOT TURNED ON YET.

        res/xml/network_security_config.xml is written and ready, but it still
        contains placeholder pins. Switching it on before real pins are in
        place would make EVERY https request fail, with no useful error.

        To turn it on, once real SPKI hashes are in that file (the file's own
        header comment explains how to generate them), add this attribute to
        the <application> tag above:

            android:networkSecurityConfig="@xml/network_security_config"
      -->

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
