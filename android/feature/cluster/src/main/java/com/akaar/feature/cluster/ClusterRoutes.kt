package com.akaar.feature.cluster

/** Assisted onboarding and pre-publication review. A queue, not an admin panel. */
object ClusterRoutes {
    const val GRAPH = "cluster"
    const val QUEUE = "cluster/queue"
    const val ARTISANS = "cluster/artisans"
    const val REVIEW = "cluster/review/{id}"
}
